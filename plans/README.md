# Implementation Plans

Generated by the improve skill on 2026-06-13. Execute in the order below unless dependencies say otherwise. Each executor: read the plan fully before starting, honor its STOP conditions, and update your row when done.

## Execution order & status

| Plan | Title | Priority | Effort | Depends on | Status |
|------|-------|----------|--------|------------|--------|
| 001 | Harden auth secret configuration and password reset logging | P1 | S | — | DONE |
| 002 | Update the vulnerable dependency chain reported by pnpm audit | P1 | M | — | DONE |
| 003 | Add characterization tests for CSV import and export behavior | P1 | S | — | DONE |
| 004 | Harden CSV import confirmation and fix attendance CSV export shape | P1 | M | 003 | DONE |

Status values: TODO | IN PROGRESS | DONE | BLOCKED (with one-line reason) | REJECTED (with one-line rationale — finding fixed independently or approach abandoned)

## Dependency notes

- Plan 004 depends on Plan 003 because the CSV behavior changes should be made against focused characterization tests first.
- Plans 001 and 002 are independent security work and can run in either order.
- Plan 003 can run in parallel with Plans 001/002 if executed in separate worktrees.

## Verification baseline from advisor recon

Repo commands from `package.json` and CI:

- `pnpm lint`
- `pnpm typecheck`
- `pnpm test`
- `pnpm build`
- `pnpm test:e2e`

Advisor run note: local `node_modules` was absent, so `pnpm lint` and `pnpm typecheck` failed with missing local binaries (`biome`/`turbo`). Executors should run `pnpm install --frozen-lockfile` first in their worktree. `pnpm audit --prod` did run and reported high/critical dependency advisories, addressed by Plan 002.

## Findings considered and rejected

- Platform admin operational overview query fan-out: observed multiple count/list queries in `apps/api/src/platform/platform-academy.service.ts`, but the current endpoint is an internal admin overview with small `limit(10)` lists; not enough evidence of a production performance problem for a P1/P2 plan.
- `dangerouslySetInnerHTML` in `apps/web/src/components/ui/chart.tsx`: likely a shadcn/Recharts CSS variable injection pattern, not user-controlled HTML; rejected as not a security finding without contrary evidence.
- Broad frontend `api as any` usage: real type-safety debt, but lower leverage than security and CSV data integrity work in this audit. Revisit in a future frontend/API-client focused audit.

## Direction options not planned yet

- Bring README V0 progress back in sync with implemented modules such as monthly fees, receipts, and student portal.
- Introduce a centralized production configuration validation module for auth, CORS, R2, email, and database settings. Plan 001 only hardens the highest-risk auth secret fallback.
