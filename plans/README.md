# Implementation Plans

Generated by the improve skill on 2026-06-13. Execute in the order below unless dependencies say otherwise. Each executor: read the plan fully before starting, honor its STOP conditions, and update your row when done.

## Execution order & status

| Plan | Title | Priority | Effort | Depends on | Status | Issue |
|------|-------|----------|--------|------------|--------|-------|
| 001 | Harden auth secret configuration and password reset logging | P1 | S | — | DONE | — |
| 002 | Update the vulnerable dependency chain reported by pnpm audit | P1 | M | — | DONE | — |
| 003 | Add characterization tests for CSV import and export behavior | P1 | S | — | DONE | — |
| 004 | Harden CSV import confirmation and fix attendance CSV export shape | P1 | M | 003 | DONE | — |
| 005 | Harden email delivery fallback and stop logging first-access email secrets | P1 | M | — | DONE | [#163](https://github.com/marcosgenesis/tatamiq/issues/163) |
| 006 | Unify QR attendance secret resolution across token generation and validation | P1 | S | — | DONE | [#164](https://github.com/marcosgenesis/tatamiq/issues/164) |
| 007 | Remove graduation eligibility query fan-out from the instructor graduation view | P1 | M | — | DONE | [#165](https://github.com/marcosgenesis/tatamiq/issues/165) |
| 008 | Restore contract-first typing in web API calls for student, settings, onboarding, and CSV flows | P2 | L | — | DONE | [#166](https://github.com/marcosgenesis/tatamiq/issues/166) |
| 009 | Sync README V0 progress with the features already shipped in the codebase | P3 | S | — | DONE | [#167](https://github.com/marcosgenesis/tatamiq/issues/167) |

Status values: TODO | IN PROGRESS | DONE | BLOCKED (with one-line reason) | REJECTED (with one-line rationale — finding fixed independently or approach abandoned)

## Dependency notes

- Plans 005, 006, and 007 are independent and can run in parallel in separate worktrees.
- Plan 008 is also logically independent, but it touches many audited web flows; prefer running it after any urgent P1 runtime fixes so code review stays manageable.
- Plan 009 is documentation-only and can run anytime, though it is easiest to review after the codebase owners agree with the current feature-status interpretation.

## Verification baseline from advisor recon

Repo commands from `package.json` and CI:

- `pnpm lint`
- `pnpm typecheck`
- `pnpm test`
- `pnpm build`
- `pnpm test:e2e`

Advisor run note on 2026-06-13:

- `pnpm lint` completed with warnings but no blocking errors
- `pnpm typecheck` passed
- `pnpm test` passed
- `pnpm audit --prod` reported no known vulnerabilities

## Findings considered and rejected

- Platform admin operational overview query fan-out: observed multiple count/list queries in `apps/api/src/platform/platform-academy.service.ts`, but the current endpoint is an internal admin overview with small `limit(10)` lists; not enough evidence of a production performance problem for a P1/P2 plan.
- `dangerouslySetInnerHTML` in `apps/web/src/components/ui/chart.tsx`: likely a standard chart-style injection pattern, not user-controlled HTML; rejected as not a security finding without contrary evidence.
- Broad frontend `api as any` usage outside the audited student/settings/onboarding/CSV flows: real debt, but intentionally deferred so Plan 008 stays bounded and reviewable.
- Lint warnings for non-null assertions and unused variables: real cleanup candidates, but lower leverage than the runtime, performance, and contract-drift findings planned here.

## Direction options not planned yet

- Build a real student notifications center on top of the existing indicator endpoints and home-screen notification affordance.
- Deepen the internal platform operations workspace with searchable support/audit timelines rather than more one-off admin views.
