# Platform provisioning creates reserved instructor accounts

Tatamiq platform administrators can provision an academy for an email that may or may not already belong to a user. If the user does not exist, the system creates a reserved authentication account without a usable password, creates the academy, assigns that account as the academy `owner`, and provides a first-access link for the instructor to define their own password; administrators must never know or set a temporary customer password.
