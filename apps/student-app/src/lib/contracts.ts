/**
 * The native app consumes domain types through this boundary. It must not import
 * UI, data hooks, or implementation details from apps/web.
 */
export type { Student, StudentMeResponse } from "@tatamiq/contracts";
